// Placeholder Telegram (bisa dipakai nanti untuk OAuth via bot)
(function(){
  const { $ } = window.App;
  const tgFab = $('#tg-fab');
  tgFab?.addEventListener('click', () => {
    // nanti bisa ganti open bot / widget
  });
})();
